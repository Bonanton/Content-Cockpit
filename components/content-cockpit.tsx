'use client'

import { useState } from 'react'
import { BarChart3, CalendarDays, ChevronDown, Image as ImageIcon, LayoutDashboard, Menu, Plus, Search, Settings2, Sparkles, Target, Users, WandSparkles, X, CheckCircle2, SlidersHorizontal, BrainCircuit, Globe2, FileText } from 'lucide-react'

type View = 'overview' | 'generator' | 'calendar' | 'analytics' | 'brands' | 'leads'
const nav = [{ id: 'overview' as View, label: 'Panoramica', icon: LayoutDashboard }, { id: 'generator' as View, label: 'Agente Hermes', icon: WandSparkles }, { id: 'calendar' as View, label: 'Calendario', icon: CalendarDays }, { id: 'analytics' as View, label: 'Analytics', icon: BarChart3 }]

export function ContentCockpit() {
  const [view, setView] = useState<View>('overview'); const [mobileOpen, setMobileOpen] = useState(false)
  const activeLabel = nav.find((item) => item.id === view)?.label || (view === 'brands' ? 'Brand' : 'Lead')
  return <div className="min-h-screen bg-background text-foreground"><aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-border bg-sidebar p-5 transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="mb-10 flex items-center justify-between px-2"><div className="flex items-center gap-2.5"><div className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Sparkles className="size-4" /></div><span className="font-semibold tracking-tight">content<span className="text-primary">pilot</span></span></div><button className="lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Chiudi menu"><X className="size-5" /></button></div><p className="mb-3 px-2 text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Workspace</p><nav className="flex flex-col gap-1">{nav.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => { setView(id); setMobileOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm transition ${view === id ? 'bg-primary text-primary-foreground shadow-sm' : 'text-muted-foreground hover:bg-accent hover:text-foreground'}`}><Icon className="size-[17px]" />{label}</button>)}</nav><p className="mb-3 mt-8 px-2 text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Gestione</p><nav className="flex flex-col gap-1"><button onClick={() => setView('brands')} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${view === 'brands' ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:bg-accent hover:text-foreground'}`}><Target className="size-[17px]" />Brand</button><button onClick={() => setView('leads')} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${view === 'leads' ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:bg-accent hover:text-foreground'}`}><Users className="size-[17px]" />Lead</button></nav><div className="mt-auto rounded-xl border border-border bg-card p-3"><div className="mb-2 flex items-center gap-2"><div className="flex size-7 items-center justify-center rounded-full bg-accent text-xs font-semibold text-primary">GC</div><div><p className="text-xs font-medium">Giulia Conti</p><p className="text-[11px] text-muted-foreground">Workspace personale</p></div></div><div className="h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full w-[72%] rounded-full bg-primary" /></div><p className="mt-2 text-[10px] text-muted-foreground">72% del piano utilizzato</p></div></aside>{mobileOpen && <button className="fixed inset-0 z-20 bg-foreground/20 lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Chiudi menu" />}<main className="lg:pl-64"><header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-border bg-background/90 px-5 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={() => setMobileOpen(true)} aria-label="Apri menu"><Menu className="size-5" /></button><div><p className="hidden text-xs text-muted-foreground sm:block">Workspace / {activeLabel}</p><h1 className="text-base font-semibold sm:text-lg">{activeLabel}</h1></div></div><div className="flex items-center gap-2"><button className="hidden rounded-lg p-2 text-muted-foreground hover:bg-accent sm:block" aria-label="Cerca"><Search className="size-[18px]" /></button><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent" aria-label="Impostazioni"><Settings2 className="size-[18px]" /></button><div className="ml-1 flex size-8 items-center justify-center rounded-full bg-accent text-xs font-semibold text-primary">GC</div></div></header><div className="mx-auto max-w-7xl p-5 md:p-8">{view === 'generator' ? <Generator /> : view === 'calendar' ? <Calendar /> : view === 'analytics' ? <Analytics /> : view === 'brands' ? <Brands /> : view === 'leads' ? <Leads /> : <Overview setView={setView} />}</div></main></div>
}

function Overview({ setView }: { setView: (v: View) => void }) { return <><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-sm text-muted-foreground">Workspace personale</p><h2 className="text-balance text-3xl font-semibold tracking-tight md:text-4xl">Buongiorno, Giulia <span className="text-primary">.</span></h2><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Hermes è pronto a trasformare la ricerca in contenuti coerenti con il tuo brand.</p></div><button onClick={() => setView('generator')} className="flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground"><Plus className="size-4" />Avvia Hermes</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Run completati','24','+18%'],['Visualizzazioni','48.2K','+24%'],['Engagement medio','6.8%','+2.4%'],['Lead generati','38','+12%']].map(([l,v,c])=><div key={l} className="rounded-xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{l}</p><div className="mt-5 flex items-end justify-between"><p className="text-3xl font-semibold">{v}</p><span className="text-xs font-medium text-primary">{c}</span></div></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.3fr_1fr]"><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><BrainCircuit className="size-5" /></div><div><h3 className="font-semibold">Hermes Research Agent</h3><p className="text-xs text-muted-foreground">Nous-style pipeline attiva</p></div><span className="ml-auto flex items-center gap-1.5 text-xs text-primary"><span className="size-2 rounded-full bg-primary" />Online</span></div><div className="mt-6 grid gap-3 sm:grid-cols-4">{['Ricerca','Brief','Creazione','QA'].map((x,i)=><div key={x} className="rounded-lg bg-muted/60 p-3"><CheckCircle2 className="mb-2 size-4 text-primary" /><p className="text-xs font-medium">{x}</p><p className="mt-1 text-[11px] text-muted-foreground">{i === 0 ? 'Web + trend' : i === 1 ? 'Brand-aware' : i === 2 ? 'Multi-canale' : 'Controllo tono'}</p></div>)}</div></div><div className="rounded-xl border border-border bg-card p-6"><p className="text-sm font-semibold">Preset attivo</p><p className="mt-2 text-lg font-semibold">Northstar Studio</p><p className="mt-1 text-sm text-muted-foreground">Design e crescita digitale</p><button onClick={() => setView('generator')} className="mt-5 flex items-center gap-2 text-xs font-medium text-primary">Configura agente <ChevronDown className="size-3 rotate-[-90deg]" /></button></div></div></> }

function Generator() { const [prompt,setPrompt]=useState(''); const [result,setResult]=useState(''); const [image,setImage]=useState(''); const [loading,setLoading]=useState(false); const [niche,setNiche]=useState('SaaS e marketing B2B'); const [brand,setBrand]=useState('Northstar Studio'); const [channel,setChannel]=useState('LinkedIn'); const [objective,setObjective]=useState('Thought leadership'); const [model,setModel]=useState('openai/gpt-4o-mini'); const [research,setResearch]=useState(true); const [steps,setSteps]=useState<string[]>([])
  async function runAgent(withImage=false){ if(!prompt.trim()) return; setLoading(true); setResult(''); setImage(''); setSteps(['Analisi nicchia e brand','Ricerca angoli e trend']); try { const res=await fetch('/api/generate-content',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,niche,brand,channel,objective,model,research,withImage})}); const data=await res.json(); setSteps(['Analisi nicchia e brand','Ricerca angoli e trend','Creazione contenuto','Controllo qualità']); setResult(data.text||'Nessun contenuto generato.'); if(data.imageUrl) setImage(data.imageUrl) } finally {setLoading(false)} }
  return <div><div className="mb-8"><div className="mb-2 flex items-center gap-2 text-sm font-medium text-primary"><BrainCircuit className="size-4" />Hermes Research Agent</div><h2 className="text-3xl font-semibold tracking-tight">Dalla ricerca al contenuto.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Configura il contesto del tuo brand e lascia che Hermes analizzi, pianifichi, scriva e prepari il visual.</p></div><div className="grid gap-5 xl:grid-cols-[380px_1fr]"><div className="space-y-4"><div className="rounded-xl border border-border bg-card p-5"><div className="mb-4 flex items-center justify-between"><h3 className="font-semibold">Configurazione agente</h3><SlidersHorizontal className="size-4 text-muted-foreground" /></div><div className="space-y-3"><Field label="Nicchia" value={niche} onChange={setNiche} /><Field label="Brand" value={brand} onChange={setBrand} /><Field label="Canale" value={channel} onChange={setChannel} options={['LinkedIn','Blog','Newsletter','Instagram']} /><Field label="Obiettivo" value={objective} onChange={setObjective} options={['Thought leadership','Acquisizione lead','SEO','Community']} /><Field label="Modello LLM" value={model} onChange={setModel} options={['openai/gpt-4o-mini','anthropic/claude-3-5-sonnet','google/gemini-2.5-flash']} /></div><label className="mt-4 flex items-center gap-2 text-xs text-muted-foreground"><input type="checkbox" checked={research} onChange={e=>setResearch(e.target.checked)} className="accent-primary" />Abilita ricerca e trend analysis</label></div><div className="rounded-xl border border-border bg-card p-5"><p className="mb-3 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Pipeline</p>{['Ricerca web e trend','Brief brand-aware','Testo multi-formato','Visual con fal','QA e suggerimenti'].map((x,i)=><div key={x} className="flex items-center gap-3 border-b border-border py-2.5 last:border-0"><span className={`flex size-5 items-center justify-center rounded-full text-[10px] ${steps[i] ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground'}`}>{steps[i] ? '✓' : i+1}</span><span className="text-xs">{x}</span></div>)}</div></div><div className="rounded-xl border border-border bg-card p-5 md:p-6"><label className="mb-2 block text-sm font-medium">Brief operativo</label><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Es. Crea un post autorevole sui 3 errori che fanno le startup B2B nel content marketing..." className="min-h-36 w-full resize-none rounded-lg border border-input bg-background p-3 text-sm leading-6 outline-none focus:ring-2 focus:ring-primary/30" /><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>runAgent(false)} disabled={loading} className="flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground disabled:opacity-50"><WandSparkles className="size-4" />{loading?'Hermes sta lavorando...':'Genera contenuto'}</button><button onClick={()=>runAgent(true)} disabled={loading} className="flex items-center gap-2 rounded-lg border border-border px-4 py-2.5 text-sm font-medium hover:bg-accent disabled:opacity-50"><ImageIcon className="size-4" />Contenuto + visual</button></div>{loading&&<div className="mt-5 rounded-lg bg-muted/60 p-4 text-xs text-muted-foreground"><div className="flex items-center gap-2"><Globe2 className="size-4 animate-pulse text-primary" />Hermes sta orchestrando la pipeline...</div></div>}{result&&<div className="mt-5 border-t border-border pt-5"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-semibold">Output pronto</p><span className="text-xs text-primary">QA completato</span></div><div className="whitespace-pre-wrap rounded-lg bg-muted/50 p-4 text-sm leading-6">{result}</div>{image&&<img src={image} alt="Visual generato da Hermes" className="mt-4 max-h-72 w-full rounded-lg object-cover" />}</div>}</div></div></div> }
function Field({label,value,onChange,options}:{label:string;value:string;onChange:(v:string)=>void;options?:string[]}){return <label className="block"><span className="mb-1.5 block text-xs text-muted-foreground">{label}</span>{options?<select value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary/30">{options.map(o=><option key={o}>{o}</option>)}</select>:<input value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary/30" />}</label>}
function Calendar(){return <Section title="Calendario editoriale" subtitle="Pianifica i contenuti creati da Hermes."><div className="grid grid-cols-7 overflow-hidden rounded-xl border border-border bg-card">{['Lun','Mar','Mer','Gio','Ven','Sab','Dom'].map(d=><div key={d} className="border-b border-border p-3 text-center text-xs font-medium text-muted-foreground">{d}</div>)}{Array.from({length:35},(_,i)=><div key={i} className="min-h-24 border-b border-r border-border p-2"><span className="text-xs text-muted-foreground">{i<7?'':i-6}</span>{i===17&&<div className="mt-2 rounded-md bg-primary/10 p-1.5 text-[10px] text-primary">Post LinkedIn</div>}</div>)}</div></Section>}
function Analytics(){return <Section title="Analytics & report" subtitle="Misura la performance del tuo sistema editoriale."><div className="grid gap-4 md:grid-cols-3">{[['Reach totale','48.240'],['Engagement','6,8%'],['Lead qualificati','38']].map(x=><div key={x[0]} className="rounded-xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{x[0]}</p><p className="mt-3 text-3xl font-semibold">{x[1]}</p><p className="mt-2 text-xs text-primary">+24% rispetto al mese scorso</p></div>)}</div></Section>}
function Brands(){return <Section title="Brand workspace" subtitle="Il contesto che Hermes usa per ogni output."><div className="rounded-xl border border-border bg-card p-6"><h3 className="text-lg font-semibold">Northstar Studio</h3><p className="mt-1 text-sm text-muted-foreground">Design, strategia e crescita digitale</p><div className="mt-6 grid gap-4 border-t border-border pt-6 md:grid-cols-3"><div><p className="text-xs text-muted-foreground">Tono</p><p className="mt-2 text-sm font-medium">Competente, diretto, umano</p></div><div><p className="text-xs text-muted-foreground">Valori</p><p className="mt-2 text-sm font-medium">Curiosità · Impatto · Chiarezza</p></div><div><p className="text-xs text-muted-foreground">Agente</p><p className="mt-2 text-sm font-medium">Hermes Research</p></div></div></div></Section>}
function Leads(){return <Section title="Lead intelligence" subtitle="Trasforma l’attenzione in relazioni di valore."><div className="rounded-xl border border-border bg-card"><div className="flex items-center gap-3 border-b border-border p-5"><Users className="size-5 text-primary"/><p className="text-sm font-medium">38 lead qualificati questo mese</p></div><div className="p-5 text-sm text-muted-foreground">I lead provenienti dai contenuti di Hermes appariranno qui.</div></div></Section>}
function Section({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <div><div className="mb-8"><h2 className="text-3xl font-semibold tracking-tight">{title}</h2><p className="mt-2 text-sm text-muted-foreground">{subtitle}</p></div>{children}</div>}
void FileText
void Target
void ChevronDown
export default ContentCockpit
